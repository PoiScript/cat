include components/ListCard
- var { config: { comm, facts, links }, lang } = htmlWebpackPlugin.options

doctype html
html(lang=lang)
  head
    title PoiScript
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
  body
    include components/Toolbar
    .ribbon
    main
      +listCard(facts, lang)
      +listCard(comm, lang)
      +listCard(links, lang)